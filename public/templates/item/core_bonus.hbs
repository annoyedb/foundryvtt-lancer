<form class="{{cssClass}}" autocomplete="off">
  {{! Sheet Header }}
  <header class="sheet-header card clipped-bot">
    <h1 class="name">
      <input
        class="lancer-input header-field"
        name="name"
        type="text"
        value="{{item.name}}"
        placeholder="{{localize 'lancer.placeholder.name'}}"
      >
    </h1>
    {{{ref-portrait-img item.img "img" item}}}
  </header>

  {{! Sheet Body }}
  <div class="flexcol">
    {{{textarea-card (upper-case (localize "lancer.common.descriptor.description.label")) "system.description"}}}
    {{{textarea-card (upper-case (localize "lancer.common.descriptor.effect.label")) "system.effect"}}}
    {{{textarea-card (upper-case (localize "lancer.mech-sheet.mounted-effect.label")) "system.mountedEffect"}}}
    {{{item-edit-arrayed-actions "system.actions" (upper-case (localize "lancer.common.descriptor.action.plural"))}}}
    {{{item-edit-arrayed-bonuses "system.bonuses"}}}
    {{{item-edit-arrayed-synergies "system.synergies" (upper-case (localize "lancer.common-sheet.synergies.label"))}}}
    {{{item-edit-arrayed-deployables "system.deployables" (upper-case (localize "TYPES.Actor.deployable.plural"))}}}
    {{{counter-array system.counters "system.counters" true}}}
    {{{item-edit-arrayed-integrated "system.integrated" (upper-case (localize "lancer.common-sheet.integrated-items.label"))}}}
  </div>
</form>
